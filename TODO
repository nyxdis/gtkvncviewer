- port to py3
- build pixmaps dir dynamically
- optional avahi support?
- gconf -> gsettings?
- german translation
- in screenshot()
  - use glib functions instead of $HOME
  - improve "Screenshot saved in" string for localization
- avoid duplicate server entries
- gnome-keyring via GIR
- gconf via GIR
